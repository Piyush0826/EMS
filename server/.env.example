# MongoDB Database
MONGODB_URL=mongodb+srv://2300031887_db_user:<db_password>@ems.wcsimbq.mongodb.net/ems?retryWrites=true&w=majority&appName=EMS

# JWT Secret Key
JWT_KEY=your-very-secure-jwt-secret-key-here

# Email Service Configuration (Gmail)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:5173

# Server Port (optional, defaults to 5000)
PORT=5000

# Notes for Gmail:
# 1. Enable "Less secure app access" in your Google account settings
# 2. Or generate an "App Password" if you have 2FA enabled
# 3. Use the 16-character app password in EMAIL_PASS field
